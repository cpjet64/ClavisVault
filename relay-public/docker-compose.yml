services:
  relay:
    command: ["--bind", "0.0.0.0:51820"]
    build:
      context: ..
      dockerfile: relay-public/Dockerfile
    container_name: clavisvault-relay
    restart: unless-stopped
    ports:
      - "51820:51820/udp"
